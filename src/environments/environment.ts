export const environment = {
  production: false,
  // HTTPS so the backend can set Secure cookies. Run dotnet dev-certs https --trust once.
  apiBaseUrl: 'https://atbest2020-001-site1.ntempurl.com'
};
